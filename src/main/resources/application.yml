spring:
  jpa:
    generate-ddl: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:postgresql://localhost:5432/bondb #jdbc:postgresql://localhost:5432/bondb
    username: richard
    password: test123
    driver-class-name: "org.postgresql.Driver"
  data:
    rest:
      basePath: /api
security:
  ignored: "/**"
  basic:
    enabled: false
management:
  security:
    enabled: false


